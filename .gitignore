# Dependencies
node_modules/
.pnp
.pnp.*

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Builds
.next/
out/
build/
dist/
*.tsbuildinfo
next-env.d.ts

# Environment
.env
.env.local
.env.*.local
!.env.example

# Data + model artifacts (regenerated by ingestion / training scripts).
# Corpora are reproducible: `marsa-ingest fixtures` offline, or the live
# ingestors where egress is permitted. The provenance manifests are the thing
# worth keeping, and those are exported into frontend/src/data/.
data/raw/
data/processed/
data/.http-cache/
# Built by `marsa-index build` — BM25 pickle, vector dump, manifest. Fully
# reproducible from the corpora; the manifest is published into
# frontend/src/data/ingestion-report.json, which IS committed.
data/index/
# Built by `marsa-graph build` — pickle + manifest, reproducible from corpora.
data/graph/
# Built by `marsa-ml train` / `congestion` — model pickle, card, port scores.
data/models/
# Written by `marsa-eval run` — the machine-readable sidecar. RESULTS.md at the
# repo root IS committed: it carries the gate stamp, and a report that only
# exists on someone's laptop cannot be checked by anyone.
data/eval/
data/*.pkl
artifacts/
*.joblib

# Logs
*.log
npm-debug.log*
yarn-error.log*

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# Test + coverage
coverage/
.coverage
htmlcov/

# Vercel
.vercel
